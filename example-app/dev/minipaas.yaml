compose:
    files:
        - ../compose.yaml
        - ../compose.build.yaml
        - compose.common.yml
        - compose.apps.yaml
        - compose.postgres.yml
        - compose.caddy.yml
namespace: sample
vars:
    MINIPAAS_DEPLOY_VERSION: 0.1.0
